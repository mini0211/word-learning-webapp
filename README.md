# 단어 학습 웹앱 프로젝트

React + Vite + Tailwind CSS 기반 영어/일본어 단어 카드 학습 앱입니다.

## 기능

- `public/words.json`에서 단어 데이터 로드
- 카드 클릭으로 앞면(단어) / 뒷면(뜻) 뒤집기
- 정답/오답 버튼
- 언어 필터: 전체 / 영어 / 일본어
- 브라우저 localStorage에 진도 자동 저장

## 실행

```powershell
npm install
npm run dev
```

## 빌드 검증

```powershell
npm run build
```
